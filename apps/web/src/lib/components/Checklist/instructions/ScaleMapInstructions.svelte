<script lang="ts">
  import { Code, Icon } from '@tableslayer/ui';
  import { IconMap } from '@tabler/icons-svelte';
</script>

<p class="checklist__instructions">
  Hold <Code code="Shift" /> and use the mouse wheel to scale the map, or open the
  <Icon Icon={IconMap} size="1rem" /><Code code="Map" /> controls and adjust the <Code code="Scale" /> input. Fine-tune the
  scale so your grid aligns perfectly with the map's grid lines.
</p>
