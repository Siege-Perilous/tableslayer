<script lang="ts">
  import { Code, Icon } from '@tableslayer/ui';
  import { IconPokerChip } from '@tabler/icons-svelte';
</script>

<p class="checklist__instructions">
  Click <Icon Icon={IconPokerChip} size="1rem" /><Code code="Marker" /> in the toolbar, then click anywhere on the map to
  place a new marker. Markers can represent characters, monsters, objects, or points of interest. Click on a marker to select
  it and edit its properties in the right panel.
</p>
